from enum import StrEnum


class LESSONTASK(StrEnum):
    INFO = "info"
    DIALOGUE = "dialogue"
    VOCAB = "vocab"
    EXPANSION = "expansion"
    GRAMMAR = "grammar"
    CHECK = "check"
    AUDIO = "audio"
    TRANSCRIBE = "transcribe"
    COMBINE_AUDIO = "combine_audio"
    LINGQ = "lingq"
